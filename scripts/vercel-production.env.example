# Copy these into Vercel → Project → Settings → Environment Variables (Production).
# Replace placeholder values; do NOT commit real secrets to git.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=
NEXT_PUBLIC_R2_PUBLIC_URL=

NEXT_PUBLIC_EVENT_DOMAIN=boracasar.net.br
NEXT_PUBLIC_APP_HOST=foto-lembranca.boracasar.net.br

ADMIN_SESSION_SECRET=

RESEND_API_KEY=
EMAIL_FROM=esqueci-a-senha@boracasar.net.br
APP_URL=https://foto-lembranca.boracasar.net.br

NEXT_PUBLIC_BILLING_CONTACT=contato@boracasar.net.br
SUBSCRIPTION_BYPASS_EMAILS=

CRON_SECRET=
RETENTION_DAYS=30

# Manual subscription SQL (Supabase):
# update public.admins set subscription_status = 'active', subscription_expires_at = now() + interval '1 month' where email = '...';

# Domains to add in Vercel → Domains:
#   foto-lembranca.boracasar.net.br
# (Path-based event URLs — no wildcard subdomain required)
